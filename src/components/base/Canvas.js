import styled from "styled-components";
import { DARK_COLOR, MEDIUM_COLOR } from "../../assets/colors";

export const Canvas = styled.canvas`
  background: ${MEDIUM_COLOR};
  border: solid 1px ${DARK_COLOR};
`;
